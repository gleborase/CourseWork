<Window x:Class="SchoolApp.View.StudentEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ученик" Height="200" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="35"/>
            <RowDefinition Height="35"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ФИО -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="ФИО:" Width="80" VerticalAlignment="Center"/>
            <TextBox x:Name="FullNameBox" Width="250"/>
        </StackPanel>

        <!-- Класс -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Класс:" Width="80" VerticalAlignment="Center"/>
            <TextBox x:Name="ClassBox" Width="250"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,10">
            <Button Content="Сохранить" Width="100" Height="30" 
                    Margin="5" Click="SaveButton_Click"/>
            <Button Content="Отмена" Width="100" Height="30" 
                    Margin="5" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>